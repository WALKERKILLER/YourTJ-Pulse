{
  "name": "@yourtj/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "protocol:check": "tsx scripts/generate-realtime.ts --check && tsx scripts/generate-twin.ts --check && tsx scripts/generate-flutter.ts --check",
    "protocol:generate": "tsx scripts/generate-realtime.ts && tsx scripts/generate-twin.ts && tsx scripts/generate-flutter.ts",
    "test": "vitest run",
    "lint": "eslint src test scripts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "tsx": "^4.23.1"
  }
}
