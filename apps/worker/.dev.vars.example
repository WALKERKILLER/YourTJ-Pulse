# 使用 `openssl rand -hex 32` 等方式生成本地随机值，不要提交真实 Token。
AUTH_TOKENS_JSON='[{"token":"<set-a-long-random-token>","user":{"id":"local-admin","displayName":"本地管理员","roles":["admin"]}}]'
CORS_ORIGINS=http://localhost:5173,http://localhost:8787
